language: python
cache: pip

python:
  - "3.5"
  - "3.6"

install:
  - pip install -r requirements.txt

jobs:
  include:
    - stage: "PEP 8 Check"
      name: "Run flake 8"
      script: if [[ $(flake8 project | wc -l) -ne 0]]; then return 1; fi
    - stage: "Tests"
      name: "Unit Tests"
      script: python manage.py cov
